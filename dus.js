
function sendMessage() {
     var messageInput =
document.getElemenByld(messageInput)


    var  message =
    messageInput.value;

     if (message.trim() !=="") {
      var messegesDiv =
document.getElementById("message");

         var messageElement =
         document.createdElement("div");
                  messageElement.textContent
= message ;

 messagesDiv.appenChild(messageElement);
        messageInput.value = "";
     }
    }
     
    






























